---
# defaults file for ansible-role-gpg2

gpg2_packages:
  - gnupg2
  - python3-pip

gpg2_module_packages:
  - packaging

gpg2_user: "{{ ansible_user }}"
gpg2_basedir : '~/.gnupg'

gpg2_install_key_files: []
gpg2_trust_level: '5'

